services:
  - type: web
    name: zeeuh-roks-chat-backend
    env: node
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: MONGO_URI
        value: your-mongodb-uri
      - key: PORT
        value: 5001
